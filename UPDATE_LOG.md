# 업데이트 기록

## 2026-05-17

- 매수/매도 수량 산정을 고정 신호 레벨 비율 중심에서 차트 강도 점수 기반 동적 비율 계산으로 변경했다.
- 매수는 `signal.score`가 높을수록 남은 현금에서 더 큰 비율을 사용하고, 낮을수록 진입 금액을 줄인다.
- 매도는 반대 로직을 적용해 상승 지속 강도가 높으면 일부만 청산하고, 강도가 약하면 더 큰 비율을 청산한다.
- 실제 포지션 청산 경로도 모멘텀과 호가 불균형을 결합한 차트 지속 강도 점수로 매도 비율을 계산하도록 보강했다.
- 전체 단위 테스트 `python3 -m pytest` 기준 `396 passed`를 확인했다.
- 대시보드 박스권 표시가 너무 좁은 가격 범위로 고정되는 문제를 개선했다.
- 대시보드 장세 판정은 업비트 ticker의 `signed_change_rate`를 우선 반영하고, 같은 현재가를 히스토리에 중복 저장하지 않는다.
- 박스권 기준을 `±0.2%`로 조정하고, 박스권 표시 폭이 너무 좁으면 최소 `0.2%` 폭으로 보정한다.
- 박스권 개선 후 전체 단위 테스트 `python3 -m pytest` 기준 `399 passed`를 확인했다.

## 2026-05-16

- 텔레그램 알림 첫 줄을 `[서버이름]` 형식으로 통일했다.
- 텔레그램 메시지를 보낼 때마다 최신 `.env`의 `SERVER_NAME`을 다시 읽도록 보강했다. 설정에서 서버 이름을 바꾸면 앱 재시작 없이 이후 알림부터 새 이름이 적용된다.
- `SERVER_NAME`이 없을 때 `APP_NAME=upbit-auto-trader`가 서버 이름처럼 표시되지 않도록 기본 폴백을 제거했다.
- 설정 화면에서 서버 이름이 비어 있으면 저장 시 현재 컴퓨터 이름을 기본 서버 이름으로 채운다.
- `./start.sh` 한 줄 실행으로 `.env` 생성, Python 3.12 이상 확인/설치, `.venv` 생성, 패키지 설치, 서버 시작까지 자동 처리하도록 보강했다.
- 대시보드 코인 거래 시뮬레이션의 운영상태와 차단 사유가 `position_checked / POSITION_HELD` 같은 내부 코드가 아니라 한국어로 표시되도록 매핑을 확장했다.
- 설정 화면에서 live 모드일 때만 업비트 액세스 키와 시크릿 키 입력 폼이 보이도록 정리했다.
- 학습 데이터 리셋 옆에 보관함 이동 없이 데이터를 완전히 삭제하는 버튼을 추가했다.
- 자동 룰 개선, 학습 완료율 초기화, 수익 성공률 기준, 룰 개선 히스토리 Markdown 보관, A/B/C 데모 룰 비교 학습, 24시간 수익률 그래프, 전문 보조지표 기반 매매 판단 보강을 반영했다.
