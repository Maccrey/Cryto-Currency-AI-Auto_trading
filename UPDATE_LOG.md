## 2026-06-10

- 하락장으로 판단된 상태에서는 `BEAR_REBOUND_PARTICIPATION` 같은 반등 예외 없이 신규 매수와 추가매수를 차단하도록 진입 가드를 강화했다. 하락장에서 상승장으로 전환된 뒤에는 확인 틱이 누적된 확정 상승장 상태에서만 진입 완화가 가능하다.
- 손절 후 재진입은 단순 쿨다운 종료만으로 허용하지 않는다. 마지막 손절 사유와 손절 체결가를 유지하고, 확정 상승장, 강한 신호, 손절가 대비 회복 가격을 모두 만족해야 다시 매수한다.
- 일반 매도 후에도 같은 가격대 즉시 재매수를 차단한다. 재진입은 매도가보다 충분히 낮은 가격 이점이 있거나, 확정 상승장과 강한 신호를 동반한 돌파 가격을 넘을 때만 허용한다.
- 익절 매도는 기본 청산 비율을 높이고 약한 신호 또는 약한 차트 흐름에서는 전량 청산하도록 조정해, 같은 가격대 반복 매도와 잔여 포지션 리스크를 줄였다.
- 24시간 수익률 차트의 시계열 포인트에 매수, 일반 매도, 손절 매도 정보를 직접 포함해 실행 내역 매칭이 어긋나도 매도/손절 마커가 표시되도록 보강했다.
- 전체 단위 테스트 `pytest` 기준 `471 passed`를 확인했고, 변경사항은 `74a49bb 하락장 매수 차단과 재진입 개선`으로 원격 `main`에 푸시했다.

## 2026-05-30 추가

- 매매 로직 업데이트 효과를 추적하기 위해 `auto_trade_cycle` 로그에 `trade_logic_update_trace`를 추가했다. 이 필드는 로직 버전, 적용 여부, 기존 차단 사유, 룰 variant 리더, 향후 비교 지표를 기록한다.
- `rule-change-history.jsonl` 이벤트에는 `optimization_tracking`을 추가해 변경 전 거래 수, 손절 수, 무거래 차단 수, 승률, 거래 공백, A/B/C 섀도우 요약과 다음 리뷰에서 비교할 최적화 지표를 남긴다.

## 2026-05-30

- 누적 `storage/logs/learning/scalping/learning.jsonl` 기준 자동매매 사이클 134,766건, 체결 이벤트 39건을 재집계했다. 마지막 체결은 2026-05-27 23:12:19+09:00이고 최신 로그는 2026-05-30 10:40:15+09:00라 24시간 이상 무거래 상태였다.
- `Codex 자동 룰 개선 시작` 리뷰에 마지막 체결 시각, 최신 이벤트 시각, 무거래 시간, 24시간 무거래 여부를 포함하고, 무거래 상태에서는 A/B/C 섀도우 우세 룰을 우선 제안하도록 했다.
- 자동매매 루프는 `AUTO_RULE_UPDATE_NO_TRADE_HOURS` 기본 24시간 동안 거래가 없고 차단 로그가 있으면 학습 충족률이 100%가 아니어도 Codex 자동 룰 개선을 시작한다.
- 매매 로직은 로그에서 룰 B 추세형이 우세했던 상승장에 한해 demo 약신호 점수 0.24 이상, 룰 B 리더, 수수료 엣지 재평가 조건이 모두 맞을 때 회복 진입을 허용하도록 좁게 개선했다.

# 업데이트 기록

## 2026-05-21

- 현재가 카드와 자동매매 진입 가드의 장세 판정 기준을 `±0.1%`로 낮춰 상승장/하락장 전환이 더 민감하게 반응하도록 했다.
- 학습 데이터가 최근 가격 기반 상승장/하락장 판정을 덮어쓰려면 더 높은 신뢰도가 필요하도록 조정해 실시간 가격 변화 반영 속도를 높였다.
- ETF 컨텍스트 URL이 비어 있을 때 설정 저장값을 `manual`이 아니라 `web`으로 기록해 공개 웹 ETF 소스가 계속 업데이트되도록 맞췄다.
- Coinglass ETF 응답이 `data.list`, `result.data.rows`처럼 중첩된 배열 구조로 내려와도 ETF 순유입/순유출과 AUM/보유량을 파싱하도록 보강했다.
- 익절 매도 기준을 고정 최소 기대 수익률이 아니라 모멘텀, 호가 불균형, 장세에 따라 동적으로 조정하도록 했다. 강한 상승장에서는 더 높은 수익률을 기다리고, 약한 하락 흐름에서는 왕복 수수료와 최소 순수익을 넘는 경우에만 더 빠르게 매도한다.
- 현재가 카드의 상승장/하락장/박스권 판정에서 과거 학습 이벤트가 현재 가격 로그 판정을 덮어쓰지 않도록 했다. Upbit 24시간 변화율이 0에 가까워도 최근 가격 로그가 충분히 움직였으면 가격 로그 기준 장세를 우선 표시한다.
- 최근 학습 로그에서 약신호 손절이 반복되면 실행 원장이 비어 있어도 약신호 신규/추가 매수를 차단하도록 했다. 현재가 카드 장세가 잠깐 상승장으로 흔들려도 최근 원본 가격 흐름이 하락이면 하락장 진입 가드를 우선 적용한다.

## 2026-05-18

- 24시간 수익률 차트에 최근 체결 원장의 매수/매도/손절 마커를 표시하도록 했다. 마커에 마우스를 올리면 체결 시각, 매수/매도 사유, 신호 강도, 점수, 체결가, 수량, 당시 수익률을 확인할 수 있다.
- 매수 체결은 진입 신호의 `signal_level`과 `signal_score`를 실행 원장에 저장하고, 매도/손절 체결은 보유 포지션의 진입 강도를 이어받아 대시보드 분석에 활용하도록 했다.
- 24시간 수익률 차트 위에 실제 업비트 가격 흐름을 주황색 점선으로 중첩 표시하고, 차트 우측에 가격 상단/하단 라벨과 하단 가격 범위를 함께 표시하도록 했다.
- 수익률 데이터가 아직 없거나 1개뿐인 초기 상태에서도 업비트 가격선 또는 현재가 점이 표시되도록 차트 시간축을 최근 24시간 기준으로 고정했다.
- 현재가 카드의 박스권 가격 범위는 관측 고저가가 현재가의 `0.2%`보다 좁으면 최소폭으로 보정되는 값임을 확인했다. 데이터가 쌓여 실제 고저폭이 최소폭보다 커지면 박스권 범위도 실제 관측 범위로 갱신된다.
- 현재까지 누적된 `storage/logs/learning/scalping/learning.jsonl` 매매 로그를 분석했다. 자동매매 사이클 27,283건 중 체결 사이클은 40건이었고, 장세별 체결은 하락장 23건, 박스권 15건, 상승장 2건이었다.
- A/B/C 데모 룰 섀도우 결과에서는 방어형 `룰 C`가 최근 구간에서 손실을 가장 작게 유지하는 흐름이 확인됐다.
- 현재가 카드의 상승장/박스권/하락장 판정을 자동매매 진입 가드에 반영했다.
- 하락장에서는 약한/중간 신호 진입을 `MARKET_STATE_BEAR_ENTRY_BLOCK`으로 차단하고, 강한 신호 이상만 예외적으로 허용한다.
- 하락장 또는 박스권에서 상승장으로 바뀐 뒤 2틱 이상 확인되면 `market_state_transition_boost`를 켜서 제한적으로 약한 신호 완화 경로를 허용한다. 단, 기존 횡보장 가드, 수수료 엣지, 재진입 제한, 쇼크 가드는 그대로 통과해야 체결된다.
- 장세 진입 판단 결과를 `auto_trade_cycle` payload에 `previous_market_state`, `market_state_transition`, `market_state_confirmation_count`, `market_state_transition_boost`로 기록하도록 했다.
- 코인거래소 시뮬레이션 컨테이너의 운용상태, 차단 사유, A/B/C 최근 액션이 내부 코드값 대신 한국어로 표시되도록 라벨 매핑을 확장했다.
- 런타임 로그, 학습 로그, 매매 원장, reset archive, 로컬 데이터셋이 Git에 올라가지 않도록 `.gitignore`에 `logs/`, `storage/`, `data/`, `*.log`를 명시했다.
- 장세 진입 가드와 자동매매 연동 테스트를 추가했고, 전체 단위 테스트 `pytest` 기준 `407 passed`를 확인했다.

## 2026-05-17

- 매수/매도 수량 산정을 고정 신호 레벨 비율 중심에서 차트 강도 점수 기반 동적 비율 계산으로 변경했다.
- 매수는 `signal.score`가 높을수록 남은 현금에서 더 큰 비율을 사용하고, 낮을수록 진입 금액을 줄인다.
- 매도는 반대 로직을 적용해 상승 지속 강도가 높으면 일부만 청산하고, 강도가 약하면 더 큰 비율을 청산한다.
- 실제 포지션 청산 경로도 모멘텀과 호가 불균형을 결합한 차트 지속 강도 점수로 매도 비율을 계산하도록 보강했다.
- 전체 단위 테스트 `python3 -m pytest` 기준 `396 passed`를 확인했다.
- 대시보드 박스권 표시가 너무 좁은 가격 범위로 고정되는 문제를 개선했다.
- 대시보드 장세 판정은 업비트 ticker의 `signed_change_rate`를 우선 반영하고, 같은 현재가를 히스토리에 중복 저장하지 않는다.
- 박스권 기준을 `±0.2%`로 조정하고, 박스권 표시 폭이 너무 좁으면 최소 `0.2%` 폭으로 보정한다.
- 박스권 개선 후 전체 단위 테스트 `python3 -m pytest` 기준 `399 passed`를 확인했다.

## 2026-05-16

- 텔레그램 알림 첫 줄을 `[서버이름]` 형식으로 통일했다.
- 텔레그램 메시지를 보낼 때마다 최신 `.env`의 `SERVER_NAME`을 다시 읽도록 보강했다. 설정에서 서버 이름을 바꾸면 앱 재시작 없이 이후 알림부터 새 이름이 적용된다.
- `SERVER_NAME`이 없을 때 `APP_NAME=upbit-auto-trader`가 서버 이름처럼 표시되지 않도록 기본 폴백을 제거했다.
- 설정 화면에서 서버 이름이 비어 있으면 저장 시 현재 컴퓨터 이름을 기본 서버 이름으로 채운다.
- `./start.sh` 한 줄 실행으로 `.env` 생성, Python 3.12 이상 확인/설치, `.venv` 생성, 패키지 설치, 서버 시작까지 자동 처리하도록 보강했다.
- 대시보드 코인 거래 시뮬레이션의 운영상태와 차단 사유가 `position_checked / POSITION_HELD` 같은 내부 코드가 아니라 한국어로 표시되도록 매핑을 확장했다.
- 설정 화면에서 live 모드일 때만 업비트 액세스 키와 시크릿 키 입력 폼이 보이도록 정리했다.
- 학습 데이터 리셋 옆에 보관함 이동 없이 데이터를 완전히 삭제하는 버튼을 추가했다.
- 트레이딩 운영시간을 `runtime-state/trading-uptime.json`에 누적 저장해 서버를 재시작해도 이어서 표시되도록 했다. 설정의 데모 트레이딩 데이터 리셋 또는 완전 데이터 삭제를 실행할 때만 운영시간 누적값을 초기화한다.
- 현재가 카드 기준 하락장에서는 보유 포지션의 추가매수를 신호 강도와 무관하게 `MARKET_STATE_BEAR_SCALE_IN_BLOCK`으로 차단한다.
- 박스권 폭이 왕복 수수료와 최소 순이익 기준을 넘고 현재가가 하단 25% 구간이면 약신호도 저점 매수 기회로 완화한다. 보유 중 현재가가 상단 25% 구간에 도달하고 진입가 대비 순이익 기준을 넘으면 `BOX_RANGE_HIGH_TAKE_PROFIT`으로 일반 매도한다.
- 자동 룰 개선, 학습 완료율 초기화, 수익 성공률 기준, 룰 개선 히스토리 Markdown 보관, A/B/C 데모 룰 비교 학습, 24시간 수익률 그래프, 전문 보조지표 기반 매매 판단 보강을 반영했다.
