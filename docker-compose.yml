services:
  trader:
    build: .
    env_file:
      - .env
    environment:
      STORAGE_DIR: /app/storage
      DASHBOARD_HOST: 0.0.0.0
      DASHBOARD_PORT: 8080
    ports:
      - "8080:8080"
    volumes:
      - ./storage:/app/storage
    restart: unless-stopped
